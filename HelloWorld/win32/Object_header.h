#pragma once

enum ENUM_STATE
{
	CSTATE_VOID,
	CSTATE_IDLE,
	CSTATE_ATTACK,
	CSTATE_RUN
};

enum ENUM_RUN
{
	RSTATE_VOID,
	RSTATE_START,
	RSTATE_ING,
	RSTATE_END
};

enum ENUM_SPEECH
{
	SPEECH_VOID,
	SPEECH_MENU,
	SPEECH_STORE
};